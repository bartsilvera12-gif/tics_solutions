# Config local de Claude Code: apunta a rutas absolutas de esta máquina
.claude/

# Credenciales: nunca al repositorio. En producción van en las
# variables de entorno del proyecto en Vercel.
.env
.env.*
!.env.example

# Dependencias de la función de contacto: las instala Vercel al desplegar
node_modules/

# Salida de tools/construir-dist.ps1: se regenera, no se versiona
dist/
